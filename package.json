{
  "name": "diegoliv",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "rm -rf .parcel-cache && parcel src/*.js --hmr-port 50619",
    "build": "parcel build src/*.js"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "alias": {
    "gsap": {
      "global": "gsap"
    },
    "SplitText": {
      "global": "SplitText"
    },
    "lenis": {
      "global": "lenis"
    }
  }
}
